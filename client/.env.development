# Local development only. Committed on purpose: it contains no secrets, and
# it is what makes `npm start` talk to the server you are running rather than
# to the deployed one.
#
# The backend must be running:  node server.js
REACT_APP_API_URL=http://localhost:5000

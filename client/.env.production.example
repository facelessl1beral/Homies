# Copy to .env.production, or set this variable in your hosting platform's
# dashboard (Vercel / Cloudflare Pages / Render -> Environment Variables).
#
# It must be the origin of your deployed API, with no trailing slash.
REACT_APP_API_URL=https://homies-api-gsg3.onrender.com
